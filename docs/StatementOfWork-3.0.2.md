---
project: palace-ios
doc_type: statement_of_work
release: 3.0.2
last_built: 2026-05-06T17:23:50Z
last_built_ref: a08b73171
last_updated: 2026-05-06T17:23:50Z
sections:
  acceptance:
    fingerprint: 5863148c
    last_verified: 2026-05-06
  contributors:
    fingerprint: 80075233
    last_verified: 2026-05-06
  deferrals:
    fingerprint: 0733b239
    last_verified: 2026-05-06
  engagement:
    fingerprint: f47f4386
    last_verified: 2026-05-06
  open_issues:
    fingerprint: todo
    last_verified: 2026-05-06
  risks_mitigations:
    fingerprint: todo
    last_verified: 2026-05-06
  scope_delivered:
    fingerprint: f82ea901
    last_verified: 2026-05-06
  stakeholders_approvals:
    fingerprint: todo
    last_verified: 2026-05-06
---

# Statement of Work — Palace iOS Core release 3.0.2

_Generated by `harness sow`. Engagement-scoped to a single Palace iOS release. Derived sections (engagement metadata, scope delivered, deferrals, contributors, acceptance) regenerate from git + ForgeOS; ledger sections (stakeholders/approvals, risks/mitigations, open issues at release) are user-filled. Layered on top of `docs/SystemRequirements.md` — refer back to FR IDs there for the durable spec._

- **Last full build:** `2026-05-06T17:23:50Z`
- **Built against ref:** `a08b73171`
- **Last updated:** `2026-05-06T17:23:50Z`

<!-- DOC:BEGIN engagement fp=f47f4386 verified=2026-05-06 -->
## Engagement

- **Release:** `3.0.2`
- **Tip ref:** `release/3.0.2` @ `a912f4a5`
- **Base ref (prior release):** `3.0.0` @ `96faa45b`
- **ForgeOS project:** `proj_87884c17`
- **ForgeOS initiative:** `init_e7603d4e`

Engagement spans the commits between **base ref** (the previous release's tag) and **tip ref** (this release's branch or tag). The SoW is rebuilt by the generator each time `harness sow update` runs; manual edits to derived sections will be overwritten.
<!-- DOC:END engagement -->

<!-- DOC:BEGIN scope_delivered fp=f82ea901 verified=2026-05-06 -->
## Scope (delivered)

Commits delivered between `3.0.0` and `release/3.0.2` (24 non-merge commits). Grouped by inferred area; refer to individual commits for full context.

### Authentication (1)

- `101f9c1a4` Fix OPDS 2 catalog sort pill, radio state, and missing book authors

### Bug fixes & user-reported issues (4)

- `61383455e` Add Adobe DRM device deauthorize to Reset Account (PP-4282 / PP-3649)
- `60e9526b5` Add patron-self-service "Reset This Library Account" button (PP-4282 / HelpSpot 17716)
- `ff189a383` Fix VoiceOver focus jump after search results announcement (PP-4029)
- `d0f39038a` Suppress Borrow Failed alert on Place Hold path (PP-4178 follow-up)

### Release prep & version bumps (13)

- `7dc5b0f86` [3.0.2] Bump MARKETING_VERSION 3.0.1 → 3.0.2 + update build marker text
- `8edb7d67e` [3.0.1] CI: gate AccountDetailCredentialStateTests behind KeychainAvailability skip
- `df4cf7938` [3.0.1] MyBooks/Holds: guard observer on hasCredentials to fix test pollution
- `a25d23c54` [3.0.1] TPPBookRegistry.sync: wait for .loaded via notification, handle in-flight load
- `b1ffe1c44` [3.0.1] TPPBookRegistry.sync: chain load() when state is .unloaded
- `d1e216805` [3.0.1] MyBooks/Holds: observe authState transitions to catch SAML re-auth
- `1485d14f9` [3.0.1] MyBooks/Holds: force reload on sign-in via didSignInPublisher
- `780c69759` [3.0.1] Defer sign-in completion to UIKit dismissal-complete callback
- `02f30d435` [3.0.1] Reset SignInModalPresenter.isPresenting only after UIKit dismissal completes
- `bf27cd7b9` [3.0.1] Fix SAML reauth modal lock-up: wait for in-flight transition before presenting
- `7c15c3002` [3.0.1] CI: skip keychain-dependent tests on entitlement-less hosts; align TPPBookCreationTests with relaxed dictionary init
- `06dd794a7` [3.0.1] SAMLReauthCoordinator: auto-present re-auth modal on /patrons/me/ 401
- `b97e0e199` [3.0.1] OPDSFeedService: recognise palaceproject.io recoverable/unrecoverable auth problem documents

### TestFlight diagnostics & hardening (4)

- `35e5ed663` [testflight] Bump build to 473 for TestFlight delivery to Adam Chandler
- `e79244ac1` [testflight] Adapt Reset Account to 3.0.1 hotfix branch + upgrade logs
- `dccdec542` [testflight] Upgrade SAML-REAUTH-FIX diagnostic logs to Log.error
- `f3adddad6` [testflight] SAML cookie-injection bandage for HelpSpot 17716 (Cornell)

### Tooling, refactors, & test infrastructure (1)

- `a912f4a5b` verify-pr: add docs-only fast-path

### Other (1)

- `0b1be0b4b` Update project.pbxproj
<!-- DOC:END scope_delivered -->

<!-- DOC:BEGIN deferrals fp=0733b239 verified=2026-05-06 -->
## Out of scope & deferrals

Items explicitly deferred from this release, extracted from `**Deferred:**` and `**Not done:**` stanzas in commit messages. These items are commitments by their author to either re-prioritize for a later release or close as won't-do.

### `7dc5b0f86` — [3.0.2] Bump MARKETING_VERSION 3.0.1 → 3.0.2 + update build marker text

_**Not done:**_

> none.

### `35e5ed663` — [testflight] Bump build to 473 for TestFlight delivery to Adam Chandler

_**Not done:**_

> TestFlight upload itself — Maurice handles archive + upload.

### `e79244ac1` — [testflight] Adapt Reset Account to 3.0.1 hotfix branch + upgrade logs

_**Not done:**_

> none.

### `dccdec542` — [testflight] Upgrade SAML-REAUTH-FIX diagnostic logs to Log.error

_**Not done:**_

> none. Defensive logging adjustment.

### `61383455e` — Add Adobe DRM device deauthorize to Reset Account (PP-4282 / PP-3649)

_**Not done:**_

> unit test that mocks `drmAuthorizer.deauthorize` and
> asserts it's called. Existing ForceResetTests cover the one-shot
> ephemeral-flag contract; a deauth-call assertion would need a
> TPPDRMAuthorizing mock. Left as a follow-up since the deauth path
> mirrors SignOut's existing well-tested pattern.

### `60e9526b5` — Add patron-self-service "Reset This Library Account" button (PP-4282 / HelpSpot 17716)

_**Not done:**_

> end-to-end UI test driving the button → alert → cleanup
> → sign-in-restart flow. Requires simdrive journey with stub auth
> backend; manual smoke recommended pre-merge given the destructive
> nature. Manual smoke checklist is in the QA review on ForgeOS
> cs_abf78649.

### `f3adddad6` — [testflight] SAML cookie-injection bandage for HelpSpot 17716 (Cornell)

_**Deferred:**_

> Test coverage for the cookie-wipe path. This is a
> verification build, not a production release. If hypothesis verified,
> add tests in the broader fix PR.

### `f3adddad6` — [testflight] SAML cookie-injection bandage for HelpSpot 17716 (Cornell)

_**Not done:**_

> No production-wide release. Verification build only.
> Targeted at TestFlight delivery to Adam Chandler's account for ticket
> 17716. Once we confirm the hypothesis (does Adam's read flow recover
> after this bandage?), we can decide whether to:
>   (a) ship the bandage broadly,
>   (b) refactor SAML re-auth to use ASWebAuthenticationSession with
>       `prefersEphemeralWebBrowserSession=true` (mirror PR #909's OIDC fix),
>   (c) defer and have CM team make /patrons/me/ less strict about SAML
>       cookie state when bearer is valid (server-side fix).

### `8edb7d67e` — [3.0.1] CI: gate AccountDetailCredentialStateTests behind KeychainAvailability skip

_**Not done:**_

> - Migrate keychain-skip pattern to a shared helper protocol so every
>   keychain-touching test class inherits it. Same dev-cleanup item
>   flagged in 7c15c3002.

### `df4cf7938` — [3.0.1] MyBooks/Holds: guard observer on hasCredentials to fix test pollution

_**Not done:**_

> - Identifying which earlier test mutates the publisher's authState
>   to leak `.loggedIn` into MyBooks tests. Probably a sign-in or
>   account-state test that doesn't reset publisher state in tearDown.
>   The guard makes the observer immune to that pollution regardless of
>   source. Filing as follow-up: tests should reset
>   `UserAccountPublisher.shared` between methods (or the publisher
>   should be injected, not singleton).
> - Develop port. Same guard needs to land on develop alongside the
>   observer (and the rest of the PR 903 chain) when the hotfix
>   forward-ports.

### `a25d23c54` — [3.0.1] TPPBookRegistry.sync: wait for .loaded via notification, handle in-flight load

_**Not done:**_

> - Identifying the path that leaves `loadingAccount` stuck after
>   sign-out (probable: a sign-out interrupt that prevents
>   BookRegistrySync.load's main-queue completion from running, leaving
>   the in-flight account label dangling). The defensive observer
>   papers over that root cause without fixing it. Worth a follow-up
>   trace once the hotfix is out — likely a sign-out path that should
>   call something equivalent to `loadingAccount = nil` before
>   resetting state.
> - Memory-leak edge case: if state never reaches `.loaded`/`.synced`,
>   the observer (and captured `completion`) is leaked. Acceptable for
>   hotfix — registries don't persist .unloaded indefinitely in any
>   observed flow, and the leak is at most one observer per stuck-sync
>   invocation.
> - Unit test. Same constraint as 1485d14f9 / d1e216805 / b1ffe1c44 —
>   end-to-end repro is the manual SAML sign-out → sign-in flow.
> - Develop port. Ports onto develop (where TPPBookRegistry has the
>   same shape) once the hotfix verification confirms this fixes the
>   device-reported issue.

### `b1ffe1c44` — [3.0.1] TPPBookRegistry.sync: chain load() when state is .unloaded

_**Not done:**_

> - Identifying which sign-out/account-change path leaves the registry
>   in `.unloaded`. The fix is defensive at the sync() boundary, not at
>   the source. Worth a follow-up trace once the hotfix is out — there
>   may be an unintended `state = .unloaded` write somewhere on the
>   sign-out path, or the load() that *should* re-run after sign-out's
>   account-change observer is missing/silently failing on this
>   particular flow. Either way the sync() guard stays the right
>   safety net.
> - Unit test. Same constraint as 1485d14f9 / d1e216805 — the chained
>   load+sync runs through `BookRegistrySync` which currently isn't
>   unit-tested in isolation; mocking it would mean stubbing
>   AccountsManager + filesystem state and is bigger than this hotfix
>   warrants. End-to-end repro is the manual SAML-reauth flow that
>   produced the user's bug.
> - Develop port. Same `.sync()` shape exists on develop; forward-port
>   after the hotfix verification confirms this fixes the reported
>   issue.

### `d1e216805` — [3.0.1] MyBooks/Holds: observe authState transitions to catch SAML re-auth

_**Not done:**_

> - Unit test. Same blocker as 1485d14f9 — the observer fires through
>   `AccountsManager.shared.currentUserAccount` which would need
>   singleton stubbing or a credentials DI seam. The wiring is exercised
>   by the same manual flow as the user's original repro: cold-launch
>   on a SAML library with stale creds → sign in via the auto-presented
>   modal → confirm My Books loads without a sign-out cycle.
> - Develop port. Same fix needs to land on develop (PR #905 has the
>   prior `didSignInPublisher` version which has the same SAML gap);
>   forward-port after hotfix is verified.

### `1485d14f9` — [3.0.1] MyBooks/Holds: force reload on sign-in via didSignInPublisher

_**Not done:**_

> - Unit test. The defensive observer fires `reloadData` / `refresh`,
>   both of which call `AccountsManager.shared.currentUserAccount` and
>   short-circuit to the SignInModal-presentation arm if the singleton's
>   credentials aren't populated. Asserting the prod path
>   (`bookRegistry.sync` was called) would require either an
>   AccountsManager singleton stub or a refactor of those methods to
>   accept a credentials check via DI — both larger than this hotfix
>   warrants. The wiring is exercised end-to-end by the same
>   manual-test flow as the SignInModal commit (sign in from book
>   detail borrow → switch to My Books → confirm books load without a
>   library toggle).
> - Develop-branch port. Same fix needs to land on develop; will be
>   filed as a follow-up commit against PR #905 or a sibling PR after
>   the hotfix is verified.

### `780c69759` — [3.0.1] Defer sign-in completion to UIKit dismissal-complete callback

_**Not done:**_

> - Unit test. Modal dismissal lifecycle is UIKit-window-state code that
>   can't be exercised without a non-trivial UIWindow/VC mock; the
>   existing presenter has no XCTest coverage for the same reason.
>   Bug is reproducible on real device by signing in from a book detail
>   borrow on a SAML library and observing the post-dismiss nav lock.
> - Develop-branch port. Same fix already landed on develop as PR #905
>   commit 8ed7451c1 (against the AppContainer-DI version of the file).
>   Port is one-way — no need to forward-port from hotfix.

### `02f30d435` — [3.0.1] Reset SignInModalPresenter.isPresenting only after UIKit dismissal completes

_**Not done:**_

> - Unit test. Modal-presentation lifecycle is UIKit-window-state code
>   that can't be exercised without a non-trivial UIWindow/VC mock; the
>   existing presenter has no XCTest coverage for the same reason. Bug
>   is reproducible by hand on real device and the fix uses a
>   documented UIHostingController extension point.

### `bf27cd7b9` — [3.0.1] Fix SAML reauth modal lock-up: wait for in-flight transition before presenting

_**Not done:**_

> - Unit test. TPPPresentationUtils is UIKit-window-state code that
>   requires a non-trivial UIWindow/VC mock to test; the existing class
>   has no XCTests because of this. Acceptable for the hotfix; the bug
>   is reproducible by hand on real device and the fix uses a
>   documented UIKit primitive.
> - Hardening other present() call sites that bypass safelyPresent. Out
>   of scope.

### `7c15c3002` — [3.0.1] CI: skip keychain-dependent tests on entitlement-less hosts; align TPPBookCreationTests with relaxed dictionary init

_**Not done:**_

> - Migrate the keychain-skip pattern to a shared helper protocol so
>   every keychain-touching test class doesn't duplicate the boilerplate.
>   Dev cleanup; not blocking the hotfix.

### `06dd794a7` — [3.0.1] SAMLReauthCoordinator: auto-present re-auth modal on /patrons/me/ 401

_**Not done:**_

> - Launch-time auto-prompt: if the app cold-starts in .credentialsStale
>   and the user does no action that hits /patrons/me/, no modal
>   surfaces. The hotfix relies on a network-driven trigger; broader
>   app-launch observation is develop-track work.
> - True session resumption: we still present the SAML web view; we
>   don't pre-fill at the IdP. The user enters Cornell's NetID/2FA
>   again. Acceptable for the hotfix; longer-term we can store and
>   replay an IdP-issued refresh artifact.
> - Forward-port to develop: needs a sibling PR against
>   fix/saml-recoverable-auth-opds (#899). Doing in a follow-up commit.

### `d0f39038a` — Suppress Borrow Failed alert on Place Hold path (PP-4178 follow-up)

_**Not done:**_

> on-device verification of Courtney's repro on The Power.
> Test matrix mirrors the scenario but a manual run on the simulator/device
> is recommended before 3.0.0 sign-off.
<!-- DOC:END deferrals -->

<!-- DOC:BEGIN stakeholders_approvals fp=todo verified=2026-05-06 -->
## Stakeholders & approvals

_(empty — TODO: fill in this section)_

> Who signs off on this release. Typical roles for a Palace release: **Engineering lead** (technical readiness), **QA / library partner validation** (functional acceptance), **Synctek release manager** (launch coordination), **Compliance** (DRM/legal sign-off if any DRM-related changes shipped). List names + dates as approvals land.
<!-- DOC:END stakeholders_approvals -->

<!-- DOC:BEGIN risks_mitigations fp=todo verified=2026-05-06 -->
## Risks & mitigations

_(empty — TODO: fill in this section)_

> Release-specific risks. Common shapes for Palace: submodule churn (PalaceAudiobookToolkit regressions), CM API drift (see `palace-cm-monitor` MCP), DRM-fulfillment regressions across Adobe RMSDK / LCP / Findaway / OverDrive / open-access paths, auth-flow flakes (SAML cookie scoping, OAuth refresh races). For each risk: probability × impact, mitigation plan, monitoring source.
<!-- DOC:END risks_mitigations -->

<!-- DOC:BEGIN open_issues fp=todo verified=2026-05-06 -->
## Open issues at release

_(empty — TODO: fill in this section)_

> Known issues at release time that don't block ship. Each entry should link to a HelpSpot ticket, JIRA, or Crashlytics issue, with a target follow-up release. Examples: TPPSettingsTests 1-second timeout flake (TPPSettingsTests.swift:57); scripts/verify-pr.sh mutation-testing baseline timeout on >600s test runs (BLOCKER #13 in mutation-testing fix-recipe).
<!-- DOC:END open_issues -->

<!-- DOC:BEGIN acceptance fp=5863148c verified=2026-05-06 -->
## Acceptance

Each commit in this release passed:
- `scripts/verify-pr.sh` (build, unit tests, test-quality lint, coverage floors, mutation testing on changed files, accessibility audit)
- ForgeOS gate pipeline (`review`, `testing`, `release`) with `forge_release_check: can_release: true`
- The harness's commit-msg hook (intent-vs-action stanza for ≥50 prod LOC) and pre-push hook (gate verification)

Release-level acceptance for this engagement is satisfied when:
- All commits referenced in the **Scope (delivered)** section above are present on the release branch
- The release branch's tip passes a fresh `scripts/verify-pr.sh`
- All deferred items (above) are tracked in JIRA or have an explicit won't-do disposition
- Crashlytics issues opened during the release window are triaged and either closed or assigned to the next release
<!-- DOC:END acceptance -->

<!-- DOC:BEGIN contributors fp=80075233 verified=2026-05-06 -->
## Contributors

24 non-merge commits between `3.0.0` and `release/3.0.2` from 1 author(s).

| Author | Commits |
| --- | ---: |
| Maurice Carrier | 24 |
<!-- DOC:END contributors -->
