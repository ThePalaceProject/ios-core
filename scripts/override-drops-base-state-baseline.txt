# Pre-existing findings amnestied when check-override-drops-base-state.py landed
# (PP-5205, 2026-09-22). Keyed `<Class>.<method>:<property>` — never a line number,
# so an unrelated edit above them does not churn this file.
#
# The gate fails on anything NOT listed here, AND on a listed entry that stops
# firing. So the amnesty can neither grow silently nor go stale.
#
# ONE entry, not two. The first draft listed `buildPlayerQueue:lastKnownPosition`
# alongside this and asserted both were "real questions" — an architect review
# showed that was wrong in substance and, worse, that baselining it would have
# PERMANENTLY REQUIRED the omission, since a baselined entry that stops firing
# fails the gate. The base's `buildPlayerQueue` assigns first-track-at-0.0
# (`OpenAccessPlayer.swift:587`), a reset to book start; the LCP override not doing
# that PRESERVES a restored position and is more correct. It carries an inline
# `// no-override-state:` instead, where the reason lives next to the code.
#
# What remains is a real defect, deferred rather than denied. The base records the
# navigation TARGET in `lastKnownPosition` before `resetPlayerQueue()`, so during
# the LCP rebuild window `currentTrackPosition` falls through to the stale old
# position — and that value reaches `savedPosition` (`OpenAccessPlayer.swift:1187`).
# Same family as the same-track freeze fixed in ios-audiobooktoolkit#226. NOT opened
# here: it is pre-existing, it is on the DRM playback path, and this branch lands on
# a release candidate already under QA test. It needs its own ticket and its own
# device pass.
LCPStreamingPlayer.rebuildPlayerQueueAndNavigate:lastKnownPosition
