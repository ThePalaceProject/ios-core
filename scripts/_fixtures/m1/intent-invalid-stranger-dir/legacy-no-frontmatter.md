# Intent: a pre-frontmatter intent, the shape 26 of the corpus still has

## Claims

- covers the changed file, but has no `---` frontmatter block

## Files in scope

- `Palace/SignInLogic/TPPReauthenticator.swift`
