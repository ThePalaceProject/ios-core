swarm_id: swarm_c2b95c85
task: "PP-4161 — in-app WKWebView streaming reader for LibrarySimplified streaming-media (text/html) OPDS publications"
intent_file: .forgeos/intent/pp-4161-streaming-html-reader.md
base_ref: feat/PP-4161-streaming-html-reader
branch: swarm/swarm_c2b95c85-scaffold
created_at: 2026-06-03
status: in-flight
forgeos:
  project_id: proj_87884c17
  initiative_id: init_cc8762f9
  changeset_id: cs_e0f586cc
  dashboard_url: https://forgeos.synctek.io/initiatives/init_cc8762f9

modules:
  - name: A-Format-Recognition
    letter: A
    risk: standard
    files_scope:
      - "Palace/Packages/PalaceCatalog/Sources/PalaceCatalog/TPPOPDSAcquisitionPath.swift (modified — add ContentTypeStreamingHTML constant + supportedTypes + supportedSubtypes leaf)"
      - "Palace/OPDS2/Models/OPDS2PublicationExtended.swift (modified — pass-through streaming-media filter at BOTH toBook sites: lightweight :270-282 and full :386-398)"
      - "Palace/Book/Models/TPPContentType.swift (modified — add .streamingHTML enum case + recognition in from(mimeType:))"
      - "Palace/Book/Models/TPPBookContentTypeConverter.swift (modified — explicit .streamingHTML case; drop default: for exhaustiveness)"
      - "Palace/Book/Models/TPPBook.swift (modified — TPPBook.isStreamingHTML computed property; verify defaultBookContentType resolves to .streamingHTML when the only leaf is streaming-media)"
      - "PalaceTests/OPDS2/OPDS2PublicationExtendedTests.swift (modified — streaming-media-only pub no longer dropped; both toBook variants)"
      - "PalaceTests/Book/TPPBookTests.swift (modified — isStreamingHTML positive + negative)"
      - "PalaceTests/Book/TPPContentTypeTests.swift (NEW — from(mimeType:) for ContentTypeStreamingHTML)"
      - "PalaceTests/Book/TPPBookContentTypeConverterTests.swift (NEW or modified — stringValue(of:) for .streamingHTML)"
      - "Palace/Packages/PalaceCatalog/Tests/PalaceCatalogTests/TPPOPDSAcquisitionPathTests.swift (modified or NEW — supportedTypes/supportedSubtypes include streaming-media)"
    contract_path: .forgeos/swarms/swarm_c2b95c85/contracts/A-Format-Recognition.md
    depends_on: []
    implementer_prompt_summary: "Land the new ContentTypeStreamingHTML MIME constant in PalaceCatalog, plumb it through TPPOPDSAcquisitionPath.supportedTypes + supportedSubtypes (under ContentTypeOPDSPublication), add the .streamingHTML case to TPPBookContentType + the converter, expose TPPBook.isStreamingHTML, and reverse the PR #847 filter at BOTH OPDS2 toBook sites. Tests-first per CLAUDE.md TDD. No UI changes."

  - name: B-StreamingReader
    letter: B
    risk: standard
    files_scope:
      - "Palace/ReaderStreaming/StreamingReaderViewController.swift (NEW — UIKit shell, WKWebView, Close bar button)"
      - "Palace/ReaderStreaming/StreamingReaderViewModel.swift (NEW — @MainActor ObservableObject, URL, progress persistence, navigation delegate)"
      - "Palace/ReaderStreaming/StreamingReaderProgressStore.swift (NEW — protocol + UserDefaults-backed implementation, key prefix palace.streamingReader.progress.<bookID>)"
      - "Palace/ReaderStreaming/StreamingReaderView.swift (NEW — SwiftUI UIViewControllerRepresentable wrapper)"
      - "Palace/Utilities/Localization/Strings.swift (modified — add Strings.StreamingReader.* namespace: close, connectionRequired, loadError)"
      - "Palace/Utilities/Testing/AccessibilityIdentifiers.swift (modified — add AccessibilityID.StreamingReader.{closeButton, webView, errorContainer})"
      - "Palace.xcodeproj/project.pbxproj (modified — add 4 new .swift files to Palace + Palace-noDRM via scripts/pbxproj_add_swift.rb; add new ReaderStreaming group)"
      - "PalaceTests/ReaderStreaming/StreamingReaderViewModelTests.swift (NEW — progress save on dismiss, restore on open, malformed-state safe handling)"
      - "PalaceTests/ReaderStreaming/StreamingReaderProgressStoreTests.swift (NEW — round-trip write/read, prefix scoping, malformed JSON fallback)"
    contract_path: .forgeos/swarms/swarm_c2b95c85/contracts/B-StreamingReader.md
    depends_on: []
    implementer_prompt_summary: "Build the new Palace/ReaderStreaming/ module: WKWebView-backed UIKit VC + SwiftUI wrapper, protocol-fronted UserDefaults progress store, ObservableObject view model. No JS bridge, no annotation layer, no font/theme controls. Online-only — render Strings.StreamingReader.connectionRequired when offline. Tests inject a FakeStreamingReaderProgressStore. Use scripts/pbxproj_add_swift.rb for project file edits — no hand-editing pbxproj."

  - name: C-BookButton-Presenter-Wiring
    letter: C
    risk: critical_path
    contract_version: 2  # v2 incorporates Phase 1a findings F1-F5 — see architect-review.md
    files_scope:
      - "Palace/MyBooks/MyBooks/BookCell/ButtonView/BookButtonType.swift (modified — add .readStreaming case + update 5 internal exhaustive switches: displaysIndicator, isDisabled, title, title(for:), buttonStyle)"
      - "Palace/MyBooks/MyBooks/BookCell/ButtonView/BookButtonsView.swift (modified — add .readStreaming accessibility ID case at line 71)"
      - "Palace/MyBooks/MyBooks/BookCell/BookCellModel.swift (modified — update 3 exhaustive switches at :478, :493, :501 to include .readStreaming; extend didSelectRead :564 switch over defaultBookContentType to add case .streamingHTML routing to coordinator)"
      - "Palace/MyBooks/MyBooks/BookCell/NormalBookCell.swift (modified — exhaustive switch at :218; add .readStreaming arm to the model.callDelegate(for:type) branch) [v2 ADDED per Phase 1a F1]"
      - "Palace/MyBooks/MyBooks/BookCell/ButtonView/BookButtonState.swift (modified — v2 Option (c): .downloadNeeded branch's inner switch over defaultBookContentType returns [.readStreaming, .return] for .streamingHTML; .downloadSuccessful + .used branches also add the case for parity)"
      - "Palace/MyBooks/LocalBookContentService.swift (modified — switch at :77; case .streamingHTML: break with comment 'streaming-HTML has no local on-device asset to delete') [v2 ADDED per Phase 1a F2]"
      - "Palace/MyBooks/BorrowOperation.swift (modified — ONE-LINE GUARD at :453: append `&& !borrowedBook.isStreamingHTML` to the existing `if attemptDownload && mapping.state == .downloadNeeded` condition; prevents auto-download of non-existent asset for streaming-HTML books that would land them in .downloadFailed) [v2.1 ADDED per Phase 1a round-2 advisory F — narrow scope exception to MyBooks/Borrow*.swift dont_touch]"
      - "Palace/Book/UI/BookDetail/BookDetailView.swift (modified — update switch at :756 to add .readStreaming branch; route to viewModel.handleAction)"
      - "Palace/Book/UI/BookDetail/BookDetailViewModel.swift (modified — update switch at :617 to add .readStreaming → didSelectReadStreaming; extend openBook switch at :847 to add case .streamingHTML routing to StreamingReaderView presentation; add didSelectReadStreaming(for:) method)"
      - "Palace/Book/UI/BookDetail/HalfSheetview.swift (modified — update BOTH switches at :94 and :123 to include .readStreaming)"
      - "Palace/Book/UI/BookDetail/BookService.swift (modified — extend dispatchOpen switch at :49 to add case .streamingHTML routing to StreamingReaderView presentation)"
      - "Palace/Book/Models/TPPBook+Extensions.swift (modified — extend format computed prop switch at :63 to add case .streamingHTML: DisplayStrings.streamingHTMLContentType; extend sample switch at :79)"
      - "Palace/CatalogUI/Views/CatalogView.swift (modified — extend resumeReading switch at :337 to handle .streamingHTML case — defensively skip with Log.warn, mirroring .audiobook handling, since RecentlyReadingService filters)"
      - "Palace/AppInfrastructure/NavigationCoordinator.swift (modified — add .streamingHTML AppRoute case if route-based presentation is chosen, OR a payload-store streamingReaderById dict — implementer chooses based on whether streaming reader is pushed or sheet-presented)"
      - "Palace/AppInfrastructure/NavigationHostView.swift (modified — add .streamingHTML route render branch if AppRoute case added)"
      - "Palace/Utilities/Localization/Strings.swift (modified — Strings.BookButton.readStreaming if a distinct label is wanted; Strings.BookDetailView.streamingHTMLContentType for format row)"
      - "Palace/Utilities/Testing/AccessibilityIdentifiers.swift (modified — AccessibilityID.BookDetail.readStreamingButton)"
      - "PalaceTests/BookStateManagement/BookButtonMapperTests.swift (modified — canonical class; add streaming-HTML cases for .downloadNeeded, .downloadSuccessful, .unregistered) [v2 FIXED per Phase 1a F4]"
      - "PalaceTests/MyBooks/BookCellModelStreamingHTMLTests.swift (NEW — didSelectRead streaming-HTML route; sits alongside existing BookCellModelOfflineTests.swift) [v2 FIXED per Phase 1a F4]"
      - "PalaceTests/Book/BookDetailViewModelTests.swift (modified — handleAction(.readStreaming) + openBook(.streamingHTML) + didSelectReadStreaming assertions) [v2 FIXED per Phase 1a F4 — actual path is PalaceTests/Book/, not PalaceTests/ViewModels/]"
      - "PalaceTests/Contract/StreamingReaderPresentationContractTests.swift (NEW — pin handleAction → present call order for streaming-HTML titles)"
      - "PalaceTests/Book/BookButtonTypeMetaTests.swift (NEW — exhaustive case coverage META regression)"
      - "PalaceTests/MyBooks/BorrowOperationStreamingHTMLTests.swift (NEW — streaming-HTML borrow does NOT trigger startDownload; EPUB borrow DOES — regression net for advisory F guard) [v2.1 ADDED]"
    contract_path: .forgeos/swarms/swarm_c2b95c85/contracts/C-BookButton-Presenter-Wiring.md
    depends_on: [A, B]
    implementer_prompt_summary: "v2 Option (c) — purely presentation. Add BookButtonType.readStreaming case, update all 9 exhaustive switches across MyBooks/ + Book/UI/BookDetail/, extend the 7 TPPBookContentType switches across Book/MyBooks/CatalogUI to handle .streamingHTML, and wire BookDetailViewModel.didSelectReadStreaming → present StreamingReaderView via NavigationCoordinator. The 'streaming = no download' semantic is the BookButtonState.downloadNeeded → [.readStreaming, .return] mapping for streamingHTML books — NO registry-state shortcut, NO new TPPBookState case, NO edits to MyBooksDownloadCenter / Borrow* / Download* / BookReturn* / Background* files. Critical-path mutation kill-rate ≥80% diff-scoped on touched MyBooks files."

  - name: D-Simdrive-Journey
    letter: D
    risk: standard
    files_scope:
      - ".simdrive/journeys/PP-4161-streaming-html-reader.yaml (NEW — catalog → sign in → search urn:uuid:84dac408-77ce-4afc-8393-9e0ced7ea3ef → book detail → Read → scroll → Close → reopen → verify saved position)"
      - ".simdrive/fixtures/baselines/3.2.0/PP-4161-streaming/ (NEW — per-step PNG + JSON baselines)"
    contract_path: .forgeos/swarms/swarm_c2b95c85/contracts/D-Simdrive-Journey.md
    depends_on: [C]
    implementer_prompt_summary: "Record a simdrive journey YAML against the repro book (urn:uuid:84dac408-77ce-4afc-8393-9e0ced7ea3ef on lyrasis-reads staging). Tier1 blocking, SSIM gating 0.85, structural checks: streaming reader chrome (Close button), web content visible. NO production code edits."

dont_touch:
  - Palace/Reader2/
  - Palace/Reader3/
  - Palace/Audiobooks/
  - Palace/SignInLogic/
  - Palace/Packages/PalaceAuth/
  - Palace/MyBooks/Borrow*.swift                           # v2.1 EXCEPT BorrowOperation.swift:453 one-line guard per advisory F
  - Palace/MyBooks/BookReturn*.swift
  - Palace/MyBooks/Download*.swift
  - Palace/MyBooks/Background*.swift                       # v2 — Option (c) excludes Background* edits
  - Palace/MyBooks/MyBooksDownloadCenter.swift             # v2 — Option (c) excludes MyBooksDownloadCenter edits
  - Palace/Network/TPPNetworkExecutor.swift
  - Palace/Network/TPPNetworkResponder.swift
  - Palace/Holds/
  - Palace/Migrations/
  - Palace/Book/UI/BookDetail/BorrowReducer.swift
  - ios-audiobooktoolkit/
  - .simdrive/_archive/

acceptance_gates:
  - "scripts/verify-pr.sh --quick passes on the merged branch"
  - "Module A: ContentTypeStreamingHTML constant exists in PalaceCatalog; supportedTypes() and supportedSubtypes(forType: ContentTypeOPDSPublication) both contain it"
  - "Module A: OPDS2 streaming-media-only publication is no longer dropped at BOTH toBook sites"
  - "Module A: TPPBookContentType.from(mimeType: ContentTypeStreamingHTML) returns .streamingHTML; TPPBook.isStreamingHTML true positive + false negative both covered"
  - "Module B: 4 new files exist in Palace/ReaderStreaming/; pbxproj has entries for Palace + Palace-noDRM targets"
  - "Module B: StreamingReaderViewModel tests instantiate the SUT (grep -c 'StreamingReaderViewModel(' ≥ 1)"
  - "Module B: StreamingReaderProgressStore round-trip + malformed-state tests exist"
  - "Module C: BookButtonType has case .readStreaming; grep -c 'case .readStreaming' across the 9 switch sites returns 9+ [v2.1 corrected per advisory G — was 8]"
  - "Module C: TPPBookContentType .streamingHTML is handled in 7 production switch sites; no NEW default: clause introduced where absent [v2.1 corrected per advisory G — was 6]"
  - "Module C: mutation kill-rate ≥80% diff-scoped on BookCellModel + BookButtonType + BookButtonState"
  - "Module C: BookButtonMapperTests covers streaming-HTML for .downloadNeeded yields [.readStreaming, .return] AND .downloadSuccessful yields .readStreaming"
  - "Module C: contract-snapshot test pins viewModel.handleAction(.readStreaming) → processingButtons.insert(.readStreaming) → coordinator.presentStreamingReader(book:) call order [v2.1 corrected per advisory H — was setState(.downloadSuccessful) which Option (c) removed]"
  - "Module C v2.1: BorrowOperation.swift:453 guard exception verified — diff contains 'isStreamingHTML' AND ≤3 insertions; BorrowOperationStreamingHTMLTests proves streamingHTML books do NOT trigger startDownload (advisory F)"
  - "Module D: .simdrive/journeys/PP-4161-streaming-html-reader.yaml validates; structural checks include 'Close', web content"
  - "All modules: zero edits to anti-scope files (dont_touch list)"
  - "Critical-path architect post-review: APPROVED (recorded in manifest.architect_review)"
  - "mcp__forgeos__forge_check_gates: architect + qa_test gates satisfied"

architect_review:
  round_1:
    reviewer_agent_id: general-purpose
    verdict: BLOCKED
    at: 2026-06-03T14:00:00Z
    review_file: .forgeos/swarms/swarm_c2b95c85/architect-review.md
    reviewed_files:
      - .forgeos/swarms/swarm_c2b95c85/plan.md
      - .forgeos/swarms/swarm_c2b95c85/contracts/A-Format-Recognition.md
      - .forgeos/swarms/swarm_c2b95c85/contracts/B-StreamingReader.md
      - .forgeos/swarms/swarm_c2b95c85/contracts/C-BookButton-Presenter-Wiring.md
      - .forgeos/swarms/swarm_c2b95c85/contracts/D-Simdrive-Journey.md
      - .forgeos/intent/pp-4161-streaming-html-reader.md
    findings:
      blocking:
        - id: F1
          title: "NormalBookCell.swift:218 missing from Contract C files_scope (exhaustive BookButtonType switch)"
          file: Palace/MyBooks/MyBooks/BookCell/NormalBookCell.swift
          fix_applied: "Added to Module C files_scope. BookButtonType switch-site count: 8 → 9."
        - id: F2
          title: "LocalBookContentService.swift:77 missing from Contract C files_scope (exhaustive TPPBookContentType switch)"
          file: Palace/MyBooks/LocalBookContentService.swift
          fix_applied: "Added to Module C files_scope. TPPBookContentType switch-site count: 6 → 7. Minimal edit: case .streamingHTML: break with explanatory comment."
        - id: F3
          title: "Linchpin 'set registry to .downloadSuccessful after streaming-HTML borrow' call site unspecified"
          file: null
          fix_applied: "Adopted reviewer-recommended Option (c) — purely presentation-layer mapping. BookButtonState.buttonTypes(book:) .downloadNeeded branch returns [.readStreaming, .return] for streamingHTML books. NO registry-state shortcut. NO new TPPBookState case. NO edits to MyBooksDownloadCenter / Borrow* / Download* / BookReturn* / Background* files. Module C scope is smaller AND honestly bounded."
        - id: F4
          title: "Three wrong test file paths in Contract C verification block"
          file: null
          fix_applied: "BookCellModelTests.swift → BookCellModelStreamingHTMLTests.swift (new); ViewModels/BookDetailViewModelTests.swift → Book/BookDetailViewModelTests.swift; Book/BookButtonMapperTests.swift (Extended class) → BookStateManagement/BookButtonMapperTests.swift (canonical class)."
        - id: F5
          title: "Internal arithmetic inconsistency: BookButtonType internal switch count (4 vs 5)"
          file: null
          fix_applied: "Standardized on 5 across plan.md §Risks #2, Contract C files_scope text, and Contract C verification grep #2 (now expects BookButtonType.swift ≥ 6: case + 5 internal switches)."
      advisory:
        - id: A
          title: "Contract A grep #6 (streaming-media presence in OPDS2PublicationExtended.swift) is too weak — passes on the existing PR #847 comment alone"
          fix_applied: "Replaced with xcodebuild test-case-pass assertion for the new testOPDS2Publication_toBook_streamingMediaOnlyAcquisition_doesNotDrop test."
        - id: B
          title: "Contract A files_scope over-claims OPDS2PublicationExtended.swift production edits — filter is generic, unblocks automatically"
          fix_applied: "Re-worded files_scope to flag test-coverage-only; production code edit at filter sites is optional (only for comment update if desired)."
        - id: C
          title: "Contract C grep #3 should iterate 7 files, not 6 (add LocalBookContentService.swift)"
          fix_applied: "Verification grep #3 in Contract C now iterates 7 files including LocalBookContentService.swift."
        - id: D
          title: "Contract D validate-journey subcommand existence unconfirmed"
          fix_applied: "Pre-flighted — harness simdrive validate-journey does NOT exist; only {status, upgrade, sessions}. Contract D verification rewritten to use mcp__simdrive__validate_replay + mcp__simdrive__replay directly."
    re_review_required: true
  round_2:
    reviewer_agent_id: general-purpose
    verdict: APPROVED
    at: 2026-06-03T14:39:00Z
    review_file: .forgeos/swarms/swarm_c2b95c85/architect-review.md
    summary: "All five blocking findings (F1-F5) and four advisories (A-D) from round 1 are cleanly resolved. Option (c) for F3 (pure presentation, no registry shortcut) is correctly adopted. Four new ADVISORY findings filed for v2 hygiene — none blocking."
    advisories:
      - id: E
        title: "Contract C BookButtonState wording — `.downloadNeeded` arm has no existing inner switch over defaultBookContentType (current code is flat if/else over authDefinition). Implementer must INTRODUCE a switch, not extend one. Re-word the files_scope text."
        severity: advisory
      - id: F
        title: "Production auto-download chain (BorrowOperation:453 + DownloadStartDispatcher:248) still fires for streaming-HTML books under Option (c). Module C scope is presentation-only — cannot patch. Needs EITHER (a) tiny scope exception with a one-line guard at BorrowOperation:453 (`&& mapping.book.defaultBookContentType != .streamingHTML`) OR (b) a Module C test driving .get → borrow → post-borrow state for a streamingHTML book and asserting bookRegistry.state remains .downloadNeeded (not .downloading or .downloadFailed) — proving graceful no-op."
        severity: advisory
        recommended_action: "Add as plan.md §Risks #10 so the implementer addresses it consciously rather than discovering mid-flight."
      - id: G
        title: "Stale acceptance gates lines 112-113 — still say '8 switch sites' and '6 production files'; should be '9' and '7' per F1+F2."
        severity: advisory
        fix: "Bump numbers in acceptance_gates to match v2 fix_applied annotations."
      - id: H
        title: "Stale acceptance gate line 116 — references 'borrow → registry.setState(.downloadSuccessful) → present' which was the round-1 approach Option (c) removed. v2 contract test #8 pins handleAction(.readStreaming) → processingButtons.insert → coordinator.presentStreamingReader."
        severity: advisory
        fix: "Re-word acceptance gate to match v2 contract test #8."
    re_review_required: false
