# Transcript — swarm_5b500284 Implementer G3 (integration)

**Status:** READY FOR COMMIT
**Implementer:** G3 (A-shrink integration)
**Date:** 2026-06-04
**Parent intent:** `.forgeos/intent/swarm-test-pollution-shrink.md`
**Predecessors:** G1 (critical-path migrations, 12 files) + G2 (bulk non-critical, 14 files)

## Summary

Folded G1 + G2 outputs into the durable surfaces:

1. Shrunk `.forgeos/swarms/swarm_47883816/A-deferred-files.txt` from **56 → 23 entries** (33 entries removed: 17 G1-cleared + 11 G2-fully-migrated + 5 promoted to whitelist).
2. Added **5 production-identity-pin files** to `AppContainerIsolationLintTests.whitelist` with per-file rationale comments.
3. Verified the lint test still passes (5 tests, 0 failures) against the new whitelist + shrunk deferred list.
4. Confirmed 4 DoD scripts exit 0 (contract reconciliation, blast radius, intent recorded, adjacency staleness).

Zero production-code touches. Lint test continues to enforce the rule against the live `PalaceTests/**` tree.

## Deferred-list shrink (56 → 23)

### Removed — fully migrated by G1 (17 files)

Cleared by G1 per `.forgeos/swarms/swarm_5b500284/transcripts/G1.md` (12 with code changes + 5 already comment-only after G1's grep audit):

| Removed from deferred list |
|---|
| PalaceTests/SignInLogic/TPPSignInOIDCTests.swift |
| PalaceTests/Audiobooks/PlaybackBootstrapperTests.swift |
| PalaceTests/Audiobooks/AudiobookSessionManagerShutdownTests.swift |
| PalaceTests/Audiobooks/AudiobookSessionStateTests.swift |
| PalaceTests/Audiobooks/AudiobookOpenStateRaceTests.swift |
| PalaceTests/Audiobooks/AudiobookSessionManagerPresenterMigrationTests.swift |
| PalaceTests/Security/DRMAdversarialTests.swift |
| PalaceTests/Audiobooks/AudiobookFirstOpenHangTests.swift |
| PalaceTests/Audiobooks/AudiobookSessionManagerFlagGatePresentationTests.swift |
| PalaceTests/MyBooks/BookReturnServiceTests.swift |
| PalaceTests/MyBooks/BookReturnServiceAuthCoordinatorTests.swift |
| PalaceTests/Network/CredentialGuardTests.swift |
| PalaceTests/Audiobook/AudiobookLoaderPredicateTests.swift |
| PalaceTests/Audiobook/Vendors/BearerTokenAdapterTests.swift |
| PalaceTests/Audiobook/Vendors/OpenAccessAdapterTests.swift |
| PalaceTests/Audiobooks/AudiobookLoaderFinalizeBuildTests.swift |
| PalaceTests/Network/TokenRefreshAndRetryQueueTests.swift |

### Removed — fully migrated by G2 (11 files)

Cleared by G2 per `.forgeos/swarms/swarm_5b500284/transcripts/G2.md` (0 code refs after migration):

| Removed from deferred list |
|---|
| PalaceTests/AppInfrastructure/AppTabHostMiniPlayerIntegrationTests.swift |
| PalaceTests/Book/BookRegistrySyncTests.swift |
| PalaceTests/Contract/BookReturnServiceContractTests.swift |
| PalaceTests/MyBooks/MyBooksDownloadCenterAccountIdThreadingTests.swift |
| PalaceTests/MyBooks/MyBooksDownloadCenterIntegrationTests.swift |
| PalaceTests/OPDS2/OPDS2CatalogWiringTests.swift |
| PalaceTests/Performance/ArraySafetyTests.swift |
| PalaceTests/Reader2/PositionSyncTests.swift |
| PalaceTests/ViewModels/CatalogLaneMoreViewModelTests.swift |
| PalaceTests/ViewModels/FacetViewModelTests.swift |
| PalaceTests/ViewModels/SettingsViewModelTests.swift |

### Removed — promoted to lint whitelist (5 files)

Production-identity-pin files where reading `AppContainer.production()` IS the test contract — see whitelist section below for per-file rationale:

| Removed from deferred list (promoted to whitelist) |
|---|
| PalaceTests/AppInfrastructure/AppContainerResetTests.swift |
| PalaceTests/AppInfrastructure/AppContainerAudiobookFactoryTests.swift |
| PalaceTests/AppInfrastructure/AppContainerImageLoaderInjectionTests.swift |
| PalaceTests/AppInfrastructure/AppContainerWithSignInModalSheetPresenterTests.swift |
| PalaceTests/AppInfrastructure/AuthCoordinatorTelemetryTests.swift |

### Remaining in deferred list (23 entries)

Categorized:

- **3 MIGRATED-DEFERRED (per-line markers, state-machine fresh-manager traps)** — G2 documented these explicitly. Tests pass via the per-line `// MIGRATED-DEFERRED:` exemption. Next swarm needs an account-detail/state-machine fixture builder before these can fully migrate:
  - `PalaceTests/Accounts/AccountStateMachineTests.swift`
  - `PalaceTests/Book/TPPBookRegistryAsyncReadinessTests.swift`
  - `PalaceTests/CarPlay/CarPlayAuthHelperReadinessTests.swift`

- **20 out-of-scope (not assigned to G1/G2 this swarm)** — Untouched by this swarm; remain queued for a follow-up A-shrink pass. Not assessed for whitelist candidacy here; the orchestrator's scope cut prioritized critical-path + bulk-non-critical.
  - `PalaceTests/Bookmarks/TPPAnnotationsTests.swift`
  - `PalaceTests/BookRegistry/TPPBookRegistryTests.swift`
  - `PalaceTests/BookStateManagement/BookCellModelActionTests.swift`
  - `PalaceTests/BookStateManagement/BookCellModelCacheInvalidationTests.swift`
  - `PalaceTests/CatalogDomain/CatalogLaneSortingTests.swift`
  - `PalaceTests/CatalogUI/CatalogSearchViewModelTests.swift`
  - `PalaceTests/CatalogUI/CatalogViewModelTests.swift`
  - `PalaceTests/MyBooks/BookCellModelOfflineTests.swift`
  - `PalaceTests/MyBooks/BookContentResetServiceTests.swift`
  - `PalaceTests/MyBooks/BookFileManagerTests.swift`
  - `PalaceTests/MyBooks/BookReturnCleverReauthTests.swift`
  - `PalaceTests/MyBooks/DiskBudgetManagerTests.swift`
  - `PalaceTests/MyBooks/DownloadFreeSpaceExhaustionTests.swift`
  - `PalaceTests/MyBooks/LocalBookContentServiceTests.swift`
  - `PalaceTests/Network/AccountAwareNetworkTests.swift`
  - `PalaceTests/Network/MultiLibraryTokenIsolationTests.swift`
  - `PalaceTests/Network/NetworkRetryTests.swift`
  - `PalaceTests/Network/ReachabilityTests.swift`
  - `PalaceTests/Network/SAMLCookieSyncTests.swift`
  - `PalaceTests/ViewModels/AccountDetailViewModelTests.swift`

## Whitelist additions (5 entries)

In `PalaceTests/MetaTests/AppContainerIsolationLintTests.swift`, added a clearly-commented swarm_5b500284 block to the `whitelist: Set<String>`:

```swift
// Added swarm_5b500284 (G2 audit, G3 promote): production-identity-pin
// files where reading `AppContainer.production()` IS the test
// contract. Migrating to `makeTestAppContainer()` would invalidate
// the assertion under test. See `.forgeos/swarms/swarm_5b500284/
// transcripts/G2.md` "Not migrated" section for per-file rationale.

"AppInfrastructure/AppContainerResetTests.swift",       // _resetForTesting() cache observation
"AppInfrastructure/AppContainerAudiobookFactoryTests.swift",          // static-cache identity across production() reads
"AppInfrastructure/AppContainerImageLoaderInjectionTests.swift",      // production graph image-loader pin
"AppInfrastructure/AppContainerWithSignInModalSheetPresenterTests.swift",  // withSignInModalSheetPresenter(_:) cache short-circuit
"AppInfrastructure/AuthCoordinatorTelemetryTests.swift",              // container.authCoordinator === again.authCoordinator
```

Each entry has its own inline comment in the file explaining WHY the production seam is load-bearing (mirrors A's whitelist style for `AppContainerTests` / `AppContainerAuthCoordinatorWiringTests` / `TPPNetworkExecutorTests`).

## DoD evidence (10 self-checks applicable)

### 1. SUT instantiation — N/A (no new SUT tests; whitelist + deferred-list changes only)

### 2. Function-result usage — N/A (no new production calls)

### 3. Multi-step test body — N/A (no new tests authored)

### 4. Scope coverage audit — G1 + G2 outputs fully folded

- All 17 G1-cleared files removed from deferred list.
- All 11 G2-cleared files removed from deferred list.
- All 5 G2-identified whitelist candidates added to whitelist with rationale.
- 3 MIGRATED-DEFERRED files retained per G2's explicit documentation.
- 20 untouched files retained per scope cut (G1/G2 did not address; not promoted/whitelisted).

### 5. Mutation pass — N/A (no production-code modifications; test-target whitelist/list edits only)

### 6. Build + lint test

```
$ xcodebuild ... build-for-testing → ** TEST BUILD SUCCEEDED **

$ xcodebuild -only-testing:PalaceTests/AppContainerIsolationLintTests test-without-building
Test Suite 'AppContainerIsolationLintTests' passed
     Executed 5 tests, with 0 failures (0 unexpected) in 0.840 seconds

xcresult: /tmp/swarm_5b500284_G3/Logs/Test/Test-Palace-2026.06.04_11-25-45--0400.xcresult
```

All 5 lint sub-tests pass:
- `testNoAppContainerProductionOutsideWhitelist` — scans live tree, finds zero violations outside whitelist+deferred+sibling-package set ✓
- `testLintCatchesSyntheticViolation` — detector self-test ✓
- `testLintIgnoresCommentLines` — comment-line exemption self-test ✓
- `testLintRespectsPerLineExemptionMarker` — `// MIGRATED-DEFERRED:` exemption self-test ✓
- `testDeferredListFileIsLoaded` — resolver reads `A-deferred-files.txt` and parses ≥1 entry (passes with 23 entries) ✓

### 7. Multi-step / wiring-claim — N/A

### 8. Contract reconciliation

```
$ python3 scripts/check-contract-reconciliation.py --quiet
$ echo $?
0
```

### 9. Blast radius

```
$ python3 scripts/check-blast-radius.py --quiet
$ echo $?
0
```

### 10. Adjacency staleness

```
$ python3 scripts/check-adjacency-staleness.py --quiet
$ echo $?
0
```

### Bonus: Intent recorded

```
$ python3 scripts/check-intent-recorded.py --quiet
$ echo $?
0
```

## Build evidence

```
xcodebuild -project Palace.xcodeproj -scheme Palace \
  -destination 'platform=iOS Simulator,id=141BD227-6E9A-4409-8D99-2D4FE818238D' \
  -derivedDataPath /tmp/swarm_5b500284_G3 \
  build-for-testing 2>&1 | tail -3
** TEST BUILD SUCCEEDED **
```

## Off-limits artifacts respected

- No production code touched (only `PalaceTests/MetaTests/AppContainerIsolationLintTests.swift` + `.forgeos/swarms/swarm_47883816/A-deferred-files.txt`)
- No `--no-verify` bypasses
- No new public API
- No force unwraps in new code
- No edits to `PalaceTests/Support/TestAppContainerFactory.swift`
- No edits to G1/G2's already-migrated test files

## Recommendation for orchestrator

**Ready for commit + push + PR.**

- Working tree contains G1 + G2 + G3 changes (test-target migrations + lint whitelist + shrunk deferred list + 4 transcripts + intent file).
- Lint test green; 4 DoD scripts exit 0.
- Build clean.
- Scope-deferral protocol respected: deferred list now contains only documented-rationale residue (3 MIGRATED-DEFERRED with state-machine trap rationale + 20 out-of-scope-this-swarm queued for next pass). No silent partial-ship.

Suggested PR title: `refactor(tests): shrink A-deferred AppContainer.production list 56→23 (swarm_5b500284)`

Suggested commit-body stanza (≥50 prod LOC threshold N/A — test-target only, but include for documentation):

```
**Not done:** 20 out-of-scope files remain on A-deferred-files.txt (Bookmarks/TPPAnnotationsTests, BookRegistry/TPPBookRegistryTests, etc. — see G3.md transcript). 3 MIGRATED-DEFERRED files (AccountStateMachineTests, TPPBookRegistryAsyncReadinessTests, CarPlayAuthHelperReadinessTests) require account-state-machine fixture builders that don't currently exist; tracked as next-swarm scope.

**Scope:** Drained 33 of the 56 deferred entries (17 G1 critical-path + 11 G2 non-critical + 5 G2 production-identity-pin promoted to whitelist). 23 entries remain (3 documented-residue + 20 untouched).
```
