# Transcript — swarm_5b500284 Implementer G1 (critical-path)

**Status:** READY FOR INTEGRATION
**Implementer:** G1 (critical-path test pollution shrink)
**Date:** 2026-06-04
**Parent intent:** `.forgeos/intent/swarm-test-pollution-shrink.md`

## Summary

Migrated `AppContainer.production()` → `makeTestAppContainer()` in 12 critical-path test files (Audiobooks / SignInLogic / MyBooks BookReturn / Network credentials / DRM). Remaining 8 files in scope were already clean (no code-level `AppContainer.production()` references — only comment refs that the lint already ignores via `trimmed.hasPrefix("//")`).

**Net change:** 104 insertions, 20 deletions across 12 files. All migrations use the `appContainer = makeTestAppContainer()` per-test pattern set up in `setUp()`/`setUpWithError()` with matching `appContainer = nil` in `tearDown()`. Zero production-code touches.

## Files modified (12)

| File | + LOC | – LOC | Sites migrated |
|---|---|---|---|
| `PalaceTests/SignInLogic/TPPSignInOIDCTests.swift` | 19 | 4 | 2 sites + setUp/tearDown added |
| `PalaceTests/Audiobooks/PlaybackBootstrapperTests.swift` | 14 | 3 | 3 sites + setUp helper |
| `PalaceTests/Audiobooks/AudiobookSessionManagerShutdownTests.swift` | 12 | 1 | 1 site + tearDown added |
| `PalaceTests/Audiobooks/AudiobookSessionStateTests.swift` | 12 | 1 | 1 site + tearDown added |
| `PalaceTests/Audiobooks/AudiobookOpenStateRaceTests.swift` | 8 | 2 | 3 sites (2 in integration test) |
| `PalaceTests/Audiobooks/AudiobookSessionManagerPresenterMigrationTests.swift` | 8 | 1 | 1 site |
| `PalaceTests/Security/DRMAdversarialTests.swift` | 7 | 1 | 1 site + setUp/tearDown |
| `PalaceTests/Audiobooks/AudiobookFirstOpenHangTests.swift` | 8 | 1 | 1 site |
| `PalaceTests/Audiobooks/AudiobookSessionManagerFlagGatePresentationTests.swift` | 7 | 1 | 1 site |
| `PalaceTests/MyBooks/BookReturnServiceTests.swift` | 5 | 3 | 1 site (inside SpyLocalContentService) |
| `PalaceTests/MyBooks/BookReturnServiceAuthCoordinatorTests.swift` | 3 | 1 | 1 site (inside SpyLocalContentService) |
| `PalaceTests/Network/CredentialGuardTests.swift` | 1 | 1 | 1 string-literal rephrase (banned-pattern inside an XCTAssert message) |

## Files in scope that needed NO change (8)

These files appear in the deferred list but contain only **comment-only** references to `AppContainer.production()` (which the lint already ignores via the `// MIGRATED-DEFERRED:` / `hasPrefix("//")` skip):

- `PalaceTests/Audiobook/AudiobookLoaderPredicateTests.swift` — single comment line in file header explaining what the predicate-extraction avoided
- `PalaceTests/Audiobook/Vendors/BearerTokenAdapterTests.swift` — comment header
- `PalaceTests/Audiobook/Vendors/OpenAccessAdapterTests.swift` — comment header
- `PalaceTests/Audiobooks/AudiobookLoaderFinalizeBuildTests.swift` — comment header
- `PalaceTests/Network/TokenRefreshAndRetryQueueTests.swift` — comment header
- `PalaceTests/MyBooks/BorrowOperationTests.swift` — zero hits (test already used dependency injection cleanly)
- `PalaceTests/MyBooks/DownloadStartCoordinatorTests.swift` — zero hits
- `PalaceTests/MyBooks/RightsManagementDispatcherTests.swift` — zero hits

G3 should remove all 20 entries from `A-deferred-files.txt`.

## DoD evidence (7 self-checks)

### 1. SUT instantiation — N/A (no new SUT tests added; only migrations)

### 2. Function-result usage — N/A (no new prod calls; pure test-target migration)

### 3. Multi-step test bodies — N/A (no new tests; existing tests preserved)

### 4. Scope coverage audit — ALL 20 files addressed

```
$ for f in <20 scope files>; do
    code_count=$(awk '!/^[[:space:]]*\/\//' "$f" | grep -c "AppContainer\.production()")
    printf "%-90s code-line=%s\n" "$f" "$code_count"
  done
```
All 20 files show `code-line=0` (zero non-comment `AppContainer.production()` references).

### 5. Mutation pass — N/A (test-target-only migrations; zero production-code touches)

### 6. Build + tests

```
$ xcodebuild ... build-for-testing
** TEST BUILD SUCCEEDED **
```

**Test-class isolation runs (post-migration, fresh build):**

| Test class | Result |
|---|---|
| AudiobookSessionManagerFlagGatePresentationTests | 4 tests, 0 failures |
| AudiobookSessionManagerPresenterMigrationTests | 8 tests, 0 failures |
| AudiobookSessionManagerShutdownTests | 8 tests, 0 failures |
| AudiobookSessionStateTransitionTests | 18 tests, 0 failures |
| AudiobookSessionErrorDescriptionTests | 4 tests, 0 failures |
| AudiobookSessionErrorTests | 2 tests, 0 failures |
| PlaybackBootstrapperTests | 8 tests, 0 failures |
| AudiobookOpenStateRaceTests | 3 tests, 0 failures |
| BorrowOperationTests | 12 tests, 0 failures |
| BookReturnServiceTests | 8 tests, 0 failures |
| BookReturnServiceAuthCoordinatorTests | 3 tests, 0 failures |
| DownloadStartCoordinatorTests | (within batch — passed) |
| RightsManagementDispatcherTests | 10 tests, 0 failures |
| OIDCViewModelSignInTests | (within batch — passed) |
| DRMAdversarialTests | 4 tests, 3 skipped (FEATURE_DRM_CONNECTOR disabled — baseline behavior), 0 failures |
| TokenRefreshAndRetryQueueTests | 9 tests, 0 failures |
| TokenRequestCredentialGuardTests | 13 tests, 0 failures |
| NetworkExecutorCredentialGuardTests | 8 tests, 0 failures |
| ConcurrentTokenRefreshTests | (within batch — passed) |
| URLSessionCredentialStorageTests | (within batch — passed) |
| BasicAuthEmptyCredentialTests | 4 tests, 0 failures |
| CredentialEdgeCaseTests | (within batch — passed) |
| TokenRefreshIntegrationTests | 2 tests, 0 failures |
| AudiobookLoaderPredicateTests | 11 tests, 0 failures |
| BearerTokenAdapterTests | 5 tests, 0 failures |
| OpenAccessAdapterTests | 6 tests, 0 failures |
| AudiobookLoaderFinalizeBuildTests | 9 tests, 0 failures |

**Cumulative xcresult bundles:**
- `/tmp/swarm_5b500284_G1/Logs/Test/Test-Palace-2026.06.04_10-36-34--0400.xcresult` — 86 tests passed
- `/tmp/swarm_5b500284_G1/Logs/Test/Test-Palace-2026.06.04_10-37-27--0400.xcresult` — 95 tests passed (3 DRM skipped)
- `/tmp/swarm_5b500284_G1/Logs/Test/Test-Palace-2026.06.04_11-06-29--0400.xcresult` — 48 tests passed (audiobook re-run)
- `/tmp/swarm_5b500284_G1/Logs/Test/Test-Palace-2026.06.04_11-07-14--0400.xcresult` — 95 tests passed (MyBooks/SignIn/Network/Security re-run)
- `/tmp/swarm_5b500284_G1/Logs/Test/Test-Palace-2026.06.04_11-07-52--0400.xcresult` — 38 tests passed (remaining)

### 7. Multi-step / wiring-claim check — N/A

### 8. Contract reconciliation

```
$ python3 scripts/check-contract-reconciliation.py --quiet
$ echo $?
0
```

### 9. Blast radius

```
$ python3 scripts/check-blast-radius.py --quiet
$ echo $?
0
```

### 10. Intent recorded

```
$ python3 scripts/check-intent-recorded.py --quiet
$ echo $?
0
```

## Surprises / regressions

### `AudiobookFirstOpenHangTests` — 3 of 5 tests crash with signal kill (PRE-EXISTING, NOT introduced by my migration)

When run in isolation as a class, 3 of 5 tests in `AudiobookFirstOpenHangTests` crash with "Test crashed with signal kill":
- `testFirstOpen_engineNotReadyAtBindTime_awaitsReadiness_beforeIssuingPlay`
- `testFirstOpen_engineNeverReady_within2s_emitsLoadError`
- `testNavBackAndReopen_secondOpenSucceeds_withoutDoublePlay`

Two pass:
- `testAwaitReadinessAndIssueFirstPlay_drivesProbeAndCommand_onProductionWiring` (PASS)
- `testAwaitReadinessAndIssueFirstPlay_timeout_surfacesLoadFailure_andNeverIssuesPlay` (PASS)

**Verified pre-existing**: I stashed my changes to AudiobookFirstOpenHangTests.swift, rebuilt, and ran the un-migrated file. Same crashes, same 3 tests. Restored my migration. The migration neither introduced nor cured this behavior. Likely root cause is the async `Task.sleep`/`ContinuousClock` pattern in those test bodies tripping the XCTest worker process — separate investigation outside my scope.

Note: when these tests are run as part of a larger batch (e.g. `-only-testing:PalaceTests/AudiobookFirstOpenHangTests -only-testing:PalaceTests/<...>`), the surviving tests in the batch DO pass. The crash is in the worker process between tests, surviving tests run in the relaunched worker.

### Files outside the deferred list

The orchestrator's scope list included 3 files NOT on `A-deferred-files.txt`: `BorrowOperationTests`, `DownloadStartCoordinatorTests`, `RightsManagementDispatcherTests`. These had zero `AppContainer.production()` references (already clean). No migration needed; included in the test-pass verification as proof they still pass.

### CredentialGuardTests string-literal banned pattern

`PalaceTests/Network/CredentialGuardTests.swift:698` had `"AppContainer.production().networkExecutor"` embedded in an XCTAssertion error message — not actual code but the lint would flag it since the line doesn't start with `//`. Rephrased the message to drop the banned substring while keeping the documentation intent ("the production networkExecutor"). No functional change.

### Spy-class init in BookReturnService tests

`SpyLocalContentService.init()` in both `BookReturnServiceTests.swift` and `BookReturnServiceAuthCoordinatorTests.swift` called `AppContainer.production().accountsManager` inside a no-arg init. Migrated to call `makeTestAppContainer().accountsManager` inside the init — every spy instance now gets a fresh container slice. Spy short-circuits real file deletion via the existing `deleteLocalContent` overrides, so the production-graph identity doesn't matter.

## Off-limits artifacts respected

- No production code touched (all changes under `PalaceTests/`)
- No edits to `PalaceTests/Support/TestAppContainerFactory.swift` (G3's territory)
- No edits to `PalaceTests/MetaTests/AppContainerIsolationLintTests.swift` (G3)
- No edits to `.forgeos/swarms/swarm_47883816/A-deferred-files.txt` (G3 removes my 20 entries)
- No `--no-verify` bypasses
- No new public API
- No force unwraps in new code

## Notes for orchestrator / integrator (G3)

1. After integration, remove all 20 G1-scope entries from `.forgeos/swarms/swarm_47883816/A-deferred-files.txt`.
2. The `AppContainerIsolationLintTests.testNoAppContainerProductionOutsideWhitelist` should now pass cleanly for my 20 scope files; verify after G2 + G3 fold in their changes.
3. The `AudiobookFirstOpenHangTests` baseline crash is a separate ticket — not blocking this swarm.
